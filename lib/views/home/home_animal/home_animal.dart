import 'package:flutter/material.dart';

class HomeAnimal extends StatelessWidget {
  const HomeAnimal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('HomeAnimal'),
    );
  }
}
